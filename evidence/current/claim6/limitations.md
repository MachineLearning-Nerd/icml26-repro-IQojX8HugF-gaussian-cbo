# Claim 6 limitations

The operation-count statement is structural: the extension uses the same two
matrix products and no projection. No wall-clock microbenchmark is used to
infer identical constant factors on every linear-algebra backend.
