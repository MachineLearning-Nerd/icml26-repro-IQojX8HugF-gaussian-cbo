# Claim 6 source audit

The retained primary TeX defines the extension as
`(I+T) Sigma0 (I+T)` for every symmetric `T`, states it is PSD, and contrasts
the unconstrained update with a projection/reflection at each step.
