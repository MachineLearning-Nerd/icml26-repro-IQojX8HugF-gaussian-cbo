# Claim 4 method

The verifier extracts `l:well` from the hash-bound complete TeX and checks the
strictly-negative moment condition structurally. The proof certificate uses
only the order facts `|m|^2 >= 0`, `||T||^2 >= 0`, and monotonicity of
expectation, yielding expectations at least zero.

The mutation control repairs `<0` to `<infinity>` and must be rejected by the
primary-source hash. This prevents a plausible editorial correction from being
mistaken for evidence about the published statement.
